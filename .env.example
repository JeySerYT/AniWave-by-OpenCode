# --- PostgreSQL ---
DB_USER=jeyseryt
DB_PASSWORD=my_strong_password_here
DB_NAME=aniwave

# --- JWT (секрет для токенов, минимум 32 символа) ---
JWT_SECRET=your-jwt-secret-key-at-least-32-characters-long

# --- OAuth (опционально, если не нужно - оставь пустыми) ---
# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# --- URL'ы (меняй только при смене домена/порта) ---
FRONTEND_URL=http://localhost
BACKEND_URL=http://localhost
